@model List<DistribuidoraSolares.Models.Proveedor>
@{
    ViewData["Title"] = "Proveedores";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Proveedores</h1>
        <a asp-action="Create" class="btn btn-primary">Nuevo Proveedor</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <form asp-action="Index" method="get" class="mb-3">
        <div class="input-group">
            <input type="text" name="buscar" class="form-control" placeholder="Buscar proveedores..." value="@ViewBag.Buscar">
            <button class="btn btn-outline-secondary" type="submit">Buscar</button>
        </div>
    </form>

    @if (Model == null || !Model.Any())
    {
        <div class="alert alert-info">
            <p>No hay proveedores registrados. <a asp-action="Create">Crea el primer proveedor</a></p>
        </div>
    }
    else
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Contacto</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var proveedor in Model)
                {
                    <tr>
                        <td>@proveedor.ProveedorId</td>
                        <td>@proveedor.Nombre</td>
                        <td>@(proveedor.Contacto ?? "-")</td>
                        <td>
                            <span class="badge @(proveedor.Estado == "ACTIVO" ? "bg-success" : "bg-secondary")">
                                @proveedor.Estado
                            </span>
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@proveedor.ProveedorId" class="btn btn-sm btn-warning">Editar</a>
                            <form asp-action="Delete" asp-route-id="@proveedor.ProveedorId" method="post" class="d-inline" onsubmit="return confirm('¿Está seguro de eliminar este proveedor?');">
                                <button type="submit" class="btn btn-sm btn-danger">Eliminar</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
